# Aggregate dependencies for local development / running the test suites on the host.
# The containers install per-service pins from app/requirements.txt and
# mockshop/requirements.txt — those are the source of truth for deployment.

# --- Agent app (Streamlit + Playwright + LangGraph) ---
streamlit==1.38.0
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
playwright==1.51.0
langgraph==0.2.44
langchain-anthropic==0.3.2
beautifulsoup4==4.12.3
anthropic==0.45.0

# --- DemoMart mock storefront (FastAPI) ---
fastapi==0.115.0
uvicorn==0.30.6
jinja2==3.1.4
itsdangerous==2.2.0
python-multipart==0.0.12
httpx==0.27.2

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
